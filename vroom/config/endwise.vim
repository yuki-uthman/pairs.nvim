
packadd vim-endwise

lua << EOF

-- require 'pairs'.setup {
  --plugins = {
    --endwise = true
  --}
--}

--local utils = require 'pairs.utils'

--require 'pairs'.setup {

  --fallback = {
    --enter = function()
      --utils.feedkey("<CR>", "n")
      --utils.feedkey("<Plug>DiscretionaryEnd" , "")

      --return ""
    --end
  --}

--}
